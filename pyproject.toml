[tool.poetry]
name = "dharma-overground"
version = "0.1.0"
description = ""
authors = ["Björn <bjoernwe@users.noreply.github.com>"]
readme = "README.md"
packages = [{include = "dharma_overground"}]

[tool.poetry.dependencies]
python = "^3.10"
Scrapy = "^2.7.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
